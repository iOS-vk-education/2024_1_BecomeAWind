enum ProfileConst {
    /// OpenCreateEvent button
    static let openCreateEventButtonSize = Const.screenWidth * 0.1
    static let openCreateEventButtonCornerRadius = openCreateEventButtonSize / 2
}
